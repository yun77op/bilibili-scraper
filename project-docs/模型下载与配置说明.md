# 模型下载与配置说明

这份文档专门说明当前项目里和“模型”相关的下载、落盘、配置与排查方式。

这里的“模型”分两类：

- 本地转写模型：`faster-whisper`
- 在线整理模型：DeepSeek `deepseek-v4-pro`

两者的工作方式不同：

- `faster-whisper` 需要把模型文件下载到本地，再由本机 CPU 或 GPU 执行转写
- DeepSeek 不下载模型到本地，只保存 API 配置，调用远端接口生成整理文章

## 1. 当前项目里有哪些模型

### 1.1 本地转写模型

当前默认使用的是：

- `Systran/faster-whisper-base`

它用于：

- 在 Bilibili 没有可用字幕时，把下载下来的音频转成文字

这个模型会真正下载到本地磁盘。

### 1.2 在线整理模型

当前默认使用的是：

- `deepseek-v4-pro`

它用于：

- 把字幕或转写稿整理成更完整的中文文章

这个模型不会下载到本地；本地只保存 API Key 和模型名。

## 2. 模型文件会下载到哪里

### 2.1 Whisper 模型目录

当前项目不使用 Hugging Face 的默认用户缓存目录，而是固定下载到项目本地：

```text
models\faster-whisper\
```

典型目录结构会像这样：

```text
models\
  faster-whisper\
    Systran-faster-whisper-base\
      config.json
      model.bin
      tokenizer.json
      vocabulary.*
```

这样做的原因：

- 避免写入 `C:\Users\yun77\.cache\huggingface` 时的权限问题
- 避免不同 Python 环境混用同一份缓存
- 便于备份、迁移和排查

### 2.2 DeepSeek 配置文件

DeepSeek 不会下载模型文件，本地只使用配置文件 [`.env.local`](C:/Users/yun77/Documents/bilibili-scraper/.env.local)：

```env
DEEPSEEK_API_KEY=...
DEEPSEEK_MODEL=deepseek-v4-pro
```

## 3. 什么时候会触发模型下载

### 3.1 Whisper

只有在这两种情况下才会触发本地模型准备：

1. 视频没有可直接使用的字幕
2. 本地还没有对应的 Whisper 模型目录

也就是说：

- 如果视频本身有字幕，通常优先直接走字幕，不会触发音频转写模型
- 如果已经下载过 `faster-whisper-base`，后面再次转写时通常不会重复下载

### 3.2 DeepSeek

DeepSeek 不存在“下载模型”这一步。

它只会在整理文章时读取本地配置：

- `DEEPSEEK_API_KEY`
- `DEEPSEEK_MODEL`

然后直接请求 DeepSeek 接口。

## 4. 当前本地配置

当前项目已经支持从 [`.env.local`](C:/Users/yun77/Documents/bilibili-scraper/.env.local) 自动加载配置。

现在这份本地配置里，和模型相关的内容是：

```env
DEEPSEEK_MODEL=deepseek-v4-pro
HF_HUB_DISABLE_XET=1
WHISPER_DEVICE=cuda
WHISPER_COMPUTE_TYPE=float16
```

如果后面要切换模型或推理设备，主要就是改这里。

## 5. Whisper 模型的配置逻辑

### 5.1 当前默认模型

当前默认本地转写模型是：

```text
base
```

它的平衡点比较适合这个项目：

- 下载体积相对可控
- 转写速度还可以
- 效果比 `tiny` 更稳

### 5.2 如何切换 Whisper 模型

如果代码里支持读取 `WHISPER_MODEL`，就可以在 [`.env.local`](C:/Users/yun77/Documents/bilibili-scraper/.env.local) 里这样写：

```env
WHISPER_MODEL=small
```

常见候选：

- `tiny`
- `base`
- `small`
- `medium`
- `large-v3`

一般规律：

- 模型越大，效果通常越好
- 模型越大，下载越慢、占用越大、转写越慢
- 对 GPU、显存、磁盘空间的要求也越高

如果你主要处理普通中文科技视频，`base` 或 `small` 往往已经够用。

## 6. CUDA / CPU 的配置方式

当前本地已经按 GPU 优先配置：

```env
WHISPER_DEVICE=cuda
WHISPER_COMPUTE_TYPE=float16
```

含义：

- `cuda`：优先使用 NVIDIA GPU
- `float16`：常见的 GPU 推理精度配置

如果后面需要切回 CPU，可以改成：

```env
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8
```

更完整的 CUDA 说明见：

- [CUDA-Whisper-配置说明.md](C:/Users/yun77/Documents/bilibili-scraper/CUDA-Whisper-%E9%85%8D%E7%BD%AE%E8%AF%B4%E6%98%8E.md)

## 7. 为什么不用 Hugging Face 默认缓存目录

之前已经实际遇到过这类问题：

- `拒绝访问 C:\Users\yun77\.cache\huggingface`
- `Permission denied ... dummy_file_src`

所以现在这套实现明确把模型目录放进项目本地，绕开默认缓存。

这比继续依赖用户级缓存更稳，尤其是在下面这些场景：

- 多个 Python 环境混用
- 某些缓存目录权限异常
- Windows 下临时目录/链接行为不稳定

## 8. 代理和模型下载

如果当前网络环境访问 Hugging Face 或相关资源不稳定，需要代理。

当前项目已经支持从 [`.env.local`](C:/Users/yun77/Documents/bilibili-scraper/.env.local) 读取：

```env
HTTP_PROXY=http://127.0.0.1:7897
HTTPS_PROXY=http://127.0.0.1:7897
BILIBILI_PROXY=http://127.0.0.1:7897
```

其中：

- `HTTP_PROXY` / `HTTPS_PROXY` 主要影响模型下载和外部 API 请求
- `BILIBILI_PROXY` 主要影响 Bilibili 接口访问和音频下载

## 9. 常见失败原因

### 9.1 DeepSeek 未配置

报错或日志类似：

- `未配置 DEEPSEEK_API_KEY，生成基础整理稿`

常见原因：

- 服务进程启动时没读到 `.env.local`
- 你访问到的是旧的 `8000` 端口服务
- 本地配置文件已改，但服务没重启

排查方法：

1. 访问 [http://127.0.0.1:8000/api/config](http://127.0.0.1:8000/api/config)
2. 看 `deepseek_configured` 是否为 `true`

### 9.2 Whisper 模型目录权限问题

报错类似：

- `Permission denied`
- `拒绝访问`

常见原因：

- 模型仍试图写入用户缓存目录
- 当前目录没有写权限
- 某次下载留下了不完整临时文件

### 9.3 CUDA 动态库缺失

报错类似：

- `cublas64_12.dll is not found or cannot be loaded`

这是 CUDA 运行库问题，不是“没有 GPU”这么简单。

解决思路已经整理在：

- [CUDA-Whisper-配置说明.md](C:/Users/yun77/Documents/bilibili-scraper/CUDA-Whisper-%E9%85%8D%E7%BD%AE%E8%AF%B4%E6%98%8E.md)

## 10. 如何确认当前模型配置已经生效

### 方法一：看服务配置接口

访问：

- [http://127.0.0.1:8000/api/config](http://127.0.0.1:8000/api/config)

确认返回值里至少有：

```json
{
  "deepseek_configured": true,
  "deepseek_model": "deepseek-v4-pro",
  "whisper_device": "cuda"
}
```

### 方法二：看实际任务日志

如果视频没有字幕，任务日志里会出现类似信息：

- 正在转写音频，本地模型：base
- 正在下载 Whisper 模型到本地目录：...

如果已经下载过模型，第二条通常不会再出现。

## 11. 建议的维护方式

后面维护这套项目时，建议按这个原则处理：

1. `.env.local` 只放配置，不放大模型文件
2. `models/` 只放本地 Whisper 模型
3. 根目录文档放项目说明，`docs/` 继续只放导出的文章
4. 改模型前先确认磁盘空间、代理和 GPU 状态
5. 改完配置后重启服务，再看 `/api/config`

## 12. 一句话总结

当前项目里真正会下载到本地的模型，主要是 `faster-whisper`；DeepSeek 不下载模型，只读取本地 API 配置。为了稳定性，Whisper 模型目录已经固定在项目内的 `models\faster-whisper\`，并通过 `.env.local` 控制设备、精度、代理和在线整理模型。
